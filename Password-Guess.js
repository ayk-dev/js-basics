function passwordGuess(pw) {
    let correctPassword = 's3cr3t!P@ssw0rd';
    let password = pw;
    if (password == correctPassword)
        console.log('Welcome');
    else
        console.log('Wrong password!') 
}